[package]
name = "config"
version = "0.1.0"
edition = "2021"
description = "Shared configuration loading for Cartridge2"

[dependencies]
# Serialization
serde = { version = "1.0", features = ["derive"] }
toml = "0.8"

# Lazy initialization
once_cell = "1.18"

# Logging
tracing = "0.1"
