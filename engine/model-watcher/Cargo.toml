[package]
name = "model-watcher"
version = "0.1.0"
edition = "2021"

[dependencies]
anyhow = "1.0"
mcts = { path = "../mcts", features = ["onnx"] }
notify = "7"
tokio = { version = "1", features = ["sync", "time", "rt"] }
tracing = "0.1"
