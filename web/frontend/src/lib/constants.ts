/**
 * Application constants
 */

/** Interval for polling stats from the server (milliseconds) */
export const STATS_POLL_INTERVAL_MS = 5000;

/** Milliseconds per second (for timestamp conversions) */
export const MS_PER_SECOND = 1000;

/** Threshold for formatting large numbers with 'k' suffix */
export const LARGE_NUMBER_THRESHOLD = 1000;
