//! Re-export centralized configuration from shared config crate.
//!
//! This module re-exports the shared configuration loading functionality
//! from the `config` crate in the engine workspace.

pub use config::*;
